import {increaseAge} from './09-mutability'


test("Increase Age", () => {
	const user = {
		name: 'Bob',
		age: 29

	}

	// const user = {
	// 	name: 'Bob',
	// 	age: 29
	//
	// }
	//
	//
	// increaseAge(user)
	//
	// expect(user.age).toBe(30)
	//
	// const superman = user
	// superman.age = 1000
	// expect(user.age).toBe(1000)

})
